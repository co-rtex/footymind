"""
Model training, evaluation, and (later) explainability for FootyMind.

- `train_model` trains and saves a multi-class classifier.
- `evaluate_model` loads the trained model and generates metrics/plots.
- `metrics` provides metric helpers.
"""
